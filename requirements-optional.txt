# OPTIONAL: semantic column-matching fallback.
#
# Adds an AI text-similarity signal that helps map unusually-named columns.
# The app works fine without it (deterministic matching + manual dropdowns);
# this is a large install (~2.5 GB, downloads a model on first use), so only
# add it where disk space and internet access allow:
#
#   venv\Scripts\pip install -r requirements-optional.txt      (Windows)
#   venv/bin/pip install -r requirements-optional.txt          (Mac)
sentence-transformers>=2.7.0
scikit-learn>=1.4.0
torch>=2.0.0
